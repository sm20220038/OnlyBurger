<?xml version="1.0" encoding="utf-8"?>
<!-- Allows plain HTTP to the backend during development.
     10.0.2.2 is the host machine as seen from the Android emulator.
     Add your PC's LAN IP here if you run the app on a physical device. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.26</domain>
        <domain includeSubdomains="true">172.25.16.1</domain>
    </domain-config>
</network-security-config>
